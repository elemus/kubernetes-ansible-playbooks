---
- hosts: "{{ target }}"
  become: yes
  roles:
    - common
    - swapoff
    - docker-install
    - kubernetes-install
    - kubectl-install
    - cluster-init
    - master-taint-remove
